{
  "name": "express",
  "version": "1.0.0",
  "scripts": {
    "serve": "node src/index.js",
    "db:migrate": "prisma migrate dev --name init",
    "up": "npm run db:migrate && npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "express": "^4.18.2",
    "express-xml-bodyparser": "^0.3.0"
  },
  "devDependencies": {
    "prisma": "^5.6.0"
  }
}
